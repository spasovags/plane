import React from 'react'
import styles from '../styles/Header.module.css'

const Header = () => {
  return (
    <div className={`${styles["overf"]}`}>
      <h1 className={`${styles["header-container"]}`}>W   e   l   c   o   m   e   t   o   m   y   b   l   o   g   !</h1>    
    </div>
  )

}

export default Header